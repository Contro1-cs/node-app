const name = 'Max';
let age = 28;
const hasHob = true;

age = 28

const summeizeUsers = (username, userAge, userHob) => {
    
    return (
    'Name is ' + username + ' age is ' + userAge + ' user has hobbies '+ userHob
    );
}

const add = (a, b) => a+b;
console.log(summeizeUsers(name , age, hasHob));